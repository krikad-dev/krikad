import 'package:flutter/material.dart';

class FeatureOnePage extends StatelessWidget {
  const FeatureOnePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Feature 1 Page'),
    );
  }
}
